import React from 'react';

const Header = () => {
  return (
    <header className="header">
      <h1 className="app-title">AssScan</h1>
      <p className="app-subtitle">Upload any exam image and convert it into clean text.</p>
    </header>
  );
};

export default Header;